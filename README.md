## React PDF template builder

1- Run `npm i && npm start`.
2- Open `./dist/report.pdf` and keep it open.
3- Open `./src/template.tsx` and edit it. 
4- Check the changes appearing live in the opened PDF document.
